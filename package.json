{
  "name": "@rabesss/cline-pass-extension",
  "version": "0.2.7",
  "description": "Dependency-free Pi and OMP provider extension for Cline Pass.",
  "type": "module",
  "license": "MIT",
  "homepage": "https://github.com/rabesss/cline-pass-extension#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rabesss/cline-pass-extension.git"
  },
  "bugs": {
    "url": "https://github.com/rabesss/cline-pass-extension/issues"
  },
  "keywords": [
    "cline",
    "cline-pass",
    "omp-plugin",
    "omp-extension",
    "pi-package",
    "pi-extension",
    "provider"
  ],
  "exports": "./dist/extension.js",
  "types": "./dist/extension.d.ts",
  "files": [
    "dist",
    "models.json",
    "scripts",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint:ts": "tsc -p tsconfig.json --noEmit",
    "lint:js": "for file in dist/*.js test/*.js test/*.mjs scripts/*.mjs; do [ -e \"$file\" ] && node --check \"$file\"; done",
    "lint": "npm run lint:ts && npm run lint:js",
    "test": "node --test test/core.test.js test/model-catalog.test.mjs && node test/omp-local.test.mjs",
    "test:omp-local": "node test/omp-local.test.mjs",
    "check": "npm run lint && npm run build && npm test",
    "check:dist": "npm run build && git diff --exit-code -- dist && test -z \"$(git ls-files --others --exclude-standard -- dist)\"",
    "models:check": "node scripts/sync-upstream-models.mjs",
    "models:proposal": "node scripts/sync-upstream-models.mjs --proposal"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18"
  },
  "omp": {
    "extensions": [
      "./dist/extension.js"
    ]
  },
  "pi": {
    "extensions": [
      "./dist/extension.js"
    ]
  }
}
